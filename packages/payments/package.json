{
  "name": "@vs/payments",
  "version": "0.1.0",
  "description": "Payment matching (SevDesk transactions ↔ bookings) for the KidBike VS booking system. Pure matching logic, plus a thin SevDesk API client used server-side only.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --experimental-strip-types \"test/*.test.ts\""
  },
  "license": "UNLICENSED"
}
